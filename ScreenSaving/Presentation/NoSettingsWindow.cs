using System.Windows;

namespace ScreenSaving.Presentation
{
    /// <summary>
    /// Represents a Window which specifies that no settings are to be shown.
    /// This Window is not actually shown, but rather is used to indicate that
    /// no settings is available.
    /// </summary>
    public class NoSettingsWindow : Window
    {
    }
}
